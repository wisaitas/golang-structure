services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: golang-structure-db
    volumes:
      - postgres-data:/var/lib/postgresql
    networks:
      - app-network

volumes:
  postgres-data:

networks:
  app-network:
    driver: bridge
