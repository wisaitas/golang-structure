docs {
  API requests mirrored from repository `httptest/*.http`.

  Choose environment **Local** and start services on the ports shown in vars (8080 golangstructure main, 3000 gatewaydummie, 8081 orchestratedummie).
}

headers {
  Accept: application/json
}
